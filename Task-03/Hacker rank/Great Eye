t = int(input())
for i in range (t):
    k = int(input())
    s = input()
    x = s.split()
    len(x)
    if k<len(x) and k>=0:
        sum = 0
        for j in x[k]:
            sum = sum + ord(j)
        print (sum)
    else:
        print("-1")
